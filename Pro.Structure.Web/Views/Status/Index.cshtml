@model IEnumerable<StatusViewModel>
@{
    ViewData["Title"] = "Statuses";
}

<div class="d-flex justify-content-between align-items-center mb-4">
    <h1>Project Statuses</h1>
    <a asp-action="Create" class="btn btn-primary">
        <i class="bi bi-plus-circle"></i> Create New Status
    </a>
</div>

<div class="card">
    <div class="card-body">
        <div class="table-responsive">
            <table class="table table-hover">
                <thead>
                    <tr>
                        <th>@Html.DisplayNameFor(m => m.Name)</th>
                        <th>@Html.DisplayNameFor(m => m.Description)</th>
                        <th>@Html.DisplayNameFor(m => m.ProjectCount)</th>
                        <th>@Html.DisplayNameFor(m => m.Created)</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                            <tr>
                                <td>
                                    <span class="status-badge @($"status-{item.Name.ToLower().Replace(" ", "-")}")">
                                        @item.Name
                                    </span>
                                </td>
                                <td>@item.Description</td>
                                <td>
                                    <span class="badge bg-info">@item.ProjectCount</span>
                                </td>
                                <td>@item.Created.ToShortDateString()</td>
                                <td>
                                    <div class="btn-group" role="group">
                                        <a asp-action="Details" asp-route-id="@item.Id" class="btn btn-sm btn-info">
                                            <i class="bi bi-info-circle"></i>
                                        </a>
                                        <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-sm btn-primary">
                                            <i class="bi bi-pencil"></i>
                                        </a>
                                        <a asp-action="Delete" asp-route-id="@item.Id" class="btn btn-sm btn-danger">
                                            <i class="bi bi-trash"></i>
                                        </a>
                                    </div>
                                </td>
                            </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div> 